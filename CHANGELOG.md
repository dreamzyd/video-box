# Changelog

## 1.0 - 2026-08-15

第一个正式稳定版本。

### Added

- 统一的登录、上传、管理和详情页面视觉设计。
- 上传文件实时进度显示，上传结束后自动跳转管理中心。
- 统一视频资源管理中心：搜索、状态、观看次数、播放、二维码、暂停/恢复、日志入口。
- 本站视频二维码按需生成，支持自定义备注、SVG / PNG 下载。
- 任意外部 URL 二维码生成工具。
- 单视频事件日志、FFmpeg 日志和转码进度。
- 资源暂停 / 恢复访问，并通过 Nginx internal + X-Accel-Redirect 防止旧媒体直链绕过权限。
- `APP_PASSWORD` 统一保护上传和管理；公开视频观看无需密码。
- CSRF 防护和 Session Cookie 安全配置。
- `BIND_ADDRESS` 网络绑定选项。

### Changed

- 项目版本从开发阶段的 v6.3 整理为正式版本 1.0。
- 默认网络绑定改为 `127.0.0.1:8080`，不再默认向公网暴露端口。
- README 重写为完整部署、升级、反代、备份、二维码和排障说明。
- Docker 镜像标签统一为 `video-box-app:1.0`。

### Compatibility

- 保留对旧 `ADMIN_PASSWORD` / `ADMIN_SESSION_HOURS` 环境变量的读取兼容。
- 旧 SQLite 数据库自动补字段，不需要删除或重新创建已有资源。
- 从 v6.3 升级时保留 `.env` 与 `data/` 即可。
